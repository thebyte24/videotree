# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Build output
# client/dist/ — committed so GoDaddy PAAS (which skips build) gets the latest frontend
client/.vite/

# Environment variables
.env
client/.env
server/.env

# Uploads (real photos — back these up separately)
server/uploads/

# Videos — too large for git
*.mp4
*.webm
*.mov

# Temp deploy folders
godaddy-deploy/
videotree-mysql-server.zip
videotree-source.zip
videotree-godaddy.zip

# OS
.DS_Store
Thumbs.db
