{
  "name": "videotree",
  "version": "1.0.0",
  "description": "VideoTree — Wedding Photography Studio.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix videotree-main/server && npm install --prefix videotree-main/client && npm run build --prefix videotree-main/client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.9.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
