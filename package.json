{
  "name": "videotree",
  "version": "1.0.0",
  "description": "VideoTree — Wedding Photography Studio.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix client && npm run build --prefix client",
    "local:client": "npm run dev --prefix client",
    "local:server": "npm run dev --prefix server",
    "local": "npm run local:server & npm run local:client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.9.7"
  }
}
